# Dependencies
backend/node_modules
frontend/node_modules

# Database file (we don't want to push this)
backend/medallion.db

# Environment variables (if we add them later)
.env

# OS files
.DS_Store
